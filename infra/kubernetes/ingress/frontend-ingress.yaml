apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: medmatch-frontend-ingress
  namespace: medmatch

  annotations:
    kubernetes.io/ingress.class: nginx

spec:
  ingressClassName: nginx

  rules:
  - http:
      paths:

      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 5173
              